import type { CSSProperties } from "react";

export const brandFontStyle: CSSProperties = {
  fontFamily:
    "var(--font-geex-display), var(--font-geex-sans), ui-sans-serif, system-ui, sans-serif",
};
